pages:
  stage: deploy
  image: python:latest
  artifacts:
    paths:
      - public
  only:
    - master
  before_script:
    - pip install pipenv
    - which pip
    - which python
  script:
    - "pipenv install"
    - "pipenv run make publish"

salt:
  stage: deploy
  image: registry.gitlab.com/saltspiro/spiro-deploy:master
  only:
    - master
  script:
    spiro-deploy astraluma prod --highstate
